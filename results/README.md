# Results

Run outputs (checkpoints, JSONL metric logs, eval tables) are generated by
`train.py` / `evaluate.py` and are not committed here. Real training happens
on Leonardo; pull back specific result files (e.g. `eval_results.json`) when
you want to check them into the repo for reference.
